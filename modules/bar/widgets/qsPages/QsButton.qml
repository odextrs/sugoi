import Quickshell
import QtQuick
import qs.widgets
import qs.config

SugoiButton {
    implicitWidth: 150
    implicitHeight: 60
    buttonBackground: Colour.secondaryContainer
    messageColor: Colour.secondary
}